# Changelog

作成日時: 2026-07-07 07:23
更新日時: 2026-07-08 14:00

## 未リリース

- 2026-07-08 17:05 システムプロンプトの「技術ブログ記事」という限定表現を外し、汎用的な「文章」に統一（校正・執筆支援・チャット・Web 検索の要約）。技術記事以外の文章にも使いやすくした。校正プロンプトは従来どおり設定 > プロンプトで上書き可能。
- 2026-07-08 16:50 チャットと執筆支援の RAG トグルを既定 ON にした（過去記事・資料を最初から文脈に含める。必要なら都度 OFF）。
- 2026-07-08 16:45 サイドバーの資料（RAG）をクリックすると、その行が選択ハイライトされるようにした（ドキュメント選択とは別管理）。左ペインの「閉じる」で選択解除。同じ資料をもう一度クリックすると選択解除＆左ペインが閉じる（トグル）。
- 2026-07-08 16:40 起動時に埋め込みモデル（Ruri）を先読みしないようにした。torch/transformers の import が重く起動直後に backend がもたつくため、初回の RAG 検索・取り込み時に遅延ロードする（エディタ利用を優先）。
- 2026-07-08 16:10 内部処理の通知（コピー / 切り取り / 貼り付け・保存・資料追加・画像追加など）を、画面右下のトーストから**下部ステータスバーの左端**に表示するように変更。短時間（約2.6秒）表示して自動で消える。右側のリソースメーターは従来どおり右寄せ。フローティングのトースト表示は廃止。
- 2026-07-08 15:55 チャットの返答末尾に生成統計を表示（tok/sec ・ トークン数 ・ 所要時間 ・ Finish reason）。アイコンは絵文字ではなくフラットな線画（雷/吹き出し/時計。currentColor でテーマ追従）。`/chat` を NDJSON 化し、本文差分に続けて最後に統計 1 行を返す。統計は llama.cpp の usage（stream_options.include_usage）と timings（predicted_per_second 等）から取得し、無ければ所要時間＝実測、tok/sec＝トークン数/所要時間で算出。
- 2026-07-08 15:35 前回選択していたワークスペースを起動時に復元するようにした（サイドバーで自動的に開いた状態に戻る）。ワークスペース ID はライブラリ固有のため、記憶はライブラリ別（localStorage `lm-selected-ws:<ライブラリパス>`）。ライブラリを切り替えると、そのライブラリで最後に開いていたワークスペースを復元する。
- 2026-07-08 15:20 表の列境界にマウスを乗せるとセルが縦に伸びて見える問題を修正。ホバー時に各セル末尾へ挿入される列リサイズハンドル（widget）が `:last-child` を奪い、段落の下マージンが復活していた。セル内ブロックのマージンを位置に依存せず一律 0 にして解消。
- 2026-07-08 15:05 Web 検索の「取り込む」で Wikipedia 等が 403 Forbidden で失敗する問題を修正。Wikimedia はブラウザ偽装の UA を robot policy で弾くため、まずブラウザ相当 UA で取得し、403/429 のときだけ素性を名乗る識別子 UA で 1 回再試行するようにした（Accept / Accept-Language も付与）。実測で Wikipedia ページの取得・本文抽出まで成功。
- 2026-07-08 14:50 書式ツールバー（フローティングバー）が画面を小さくすると右に大きく見切れる問題を修正。バーの実寸で右端・下端が収まるよう自動で左・上へ寄せる（ウィンドウ高さの変更にも追従）。バーの幅が画面幅より広い場合は左端固定で右がはみ出す（許容）。 返答を本文へ「挿入」「置換」するボタンを削除（会話だけできればよい、というユーザー判断）。返答の Markdown 表示・ストリーミングはそのまま。
- 2026-07-08 14:20 チャットの返答表示を調整: 吹き出しをやめ、返答を Markdown として整形表示（見出し・箇条書き・コードブロック・リンク等）。ストリーミング表示は維持（トークンが届くたびに再レンダー）。ユーザー発言は右寄せの淡い吹き出しのまま区別。
- 2026-07-08 14:00 LLM とのチャット機能を追加（フェーズ 7 の第一歩）。上部ツールバーの「チャット」ボタンで右ペインに会話パネルが開き、**編集中の記事（＋選択範囲）を文脈に** Gemma 4 とマルチターンで相談・レビューできる。応答はストリーミング表示。各返答に「挿入」（カーソル位置）「置換」（選択範囲）ボタン。送信欄に **RAG トグル**（既定 OFF。過去記事・資料を検索して文脈に含める）。右ペインは「執筆支援」「チャット」のタブ切替式に。**ネット検索・LLM の自動判断（function calling）は今回は入れていない**（後続で検討）。
- 2026-07-08 13:00 表（テーブル）機能を追加。書式ツールバーの「表」ボタンで 3×3（ヘッダ行あり）を挿入でき、列幅はドラッグでリサイズ可能。カーソルが表の中にあるときだけ書式パレット下段に表操作バー（行/列の追加・削除、セル結合/分割、ヘッダ行切替、表の削除）が出る。Markdown（GFM）と相互変換する（読み込み・貼り付けで `| … |` を表に、書き出しで表を `| … |` に）。**制約**: セル結合や複数段落セルは GFM で表現できず、Markdown 書き出し時に落ちる。GFM の仕様上ヘッダ行が必須。
- 2026-07-08 12:45 校正ガイド `docs/design/proofreading.md` を作成（校正の仕組み・システムプロンプトの上書き方法・敬体/常体などの文体変換の書き方・時制変更の注意）。
- 2026-07-08 12:20 設定・ライブラリレジストリ・LLM の PID 記録の保存先を、PC のユーザー環境（`~/.lm-text-editor/`）から**リポジトリの `data/`** に変更（配布前のため一箇所にまとめる）。旧場所の設定・レジストリは初回起動時に自動で `data/` へ引き継ぐ。`data/` は gitignore 済み。
- 2026-07-08 12:00 資料（RAG）のバッジ色を種別によらず一律の落ち着いたグレーに統一（ライト #6c727a / ダーク #3f454d。種別は M/R/W/A の文字で区別）。README.md を作成。
- 2026-07-08 11:55 タイトルをクリックすると出ていた紫の矩形（フォーカスリング）を非表示に。枠なしの見出し欄で、リングの上辺が固定ツールバーの下に食い込んで中途半端に見えていた問題を解消。
- 2026-07-08 11:45 コンテキスト長スライダーを刷新: トラックを細く・短くし、現在値ボックスの左にゴミ箱アイコン（押すと既定 16k に戻す）を配置。目盛りをつまみ中心に正確に合わせた。「既定値に戻す UI」をデザインガイドラインにルール化。
- 2026-07-08 11:30 モデルロード時にピルが「起動中→モデルをロードしてください→起動中」とちらつく問題を修正（旧→新サーバ切替の一瞬に出る stopped 状態を、ロード中は無視するようにした）。
- 2026-07-08 11:20 設定ウィンドウを拡大（920×660）。設定に「プロンプト」セクションを追加し、校正のシステムプロンプトを編集できるようにした（空にして保存で既定に戻る / 「既定に戻す」ボタンあり）。
- 2026-07-08 11:00 モデルピルの幅をモデル名に合わせた可変（短ければ小さく、長ければ上限まで広がる）に。右にイジェクト（アンロード）ボタンを追加（未ロード時は非アクティブ）。ロード中のシマー紫を控えめに。
- 2026-07-08 10:45 前セッションで停止しきれず :8080 に居残った / 外部起動の llama-server を、アプリから停止・引き取りできるように修正。以前は「外部起動」と判定されるとロードもアンロードもできず操作不能になっていた。モデル切替・アンロード・アプリ終了時に、自ポートの llama-server（プロセス名で検証）を停止する。外部起動サーバのモデル名もピルに表示。
- 2026-07-08 10:20 モデル選択バーをシンプルなピル型に刷新（video-content-analyzer 参考）。中央に「⚙ モデル名 ⌄」のピルを置き、クリックでモデル選択モーダルを開く（一覧から選んで「ロード」/「アンロード」）。ロード済みはピルをキーカラー（紫）で塗り、ロード中はシマー表示。
- 2026-07-08 10:00 資料（RAG）の「新規作成」を再編集できるようにした。＋メニューの「新規作成」でその場で空のメモが作られサイドバーに追加され、左ペインがそのまま Markdown 編集画面になる。保存すると段落ごとにチャンク化して検索へ再登録（本文は編集用に別途保持）。既存のメモはサイドバーから開いて再編集・保存できる。Web / ファイル取り込みの資料は従来どおり閲覧のみ。
- 2026-07-07 17:30 埋め込みモデル（Ruri v3）を完全オフラインで読み込むように変更（起動時の「unauthenticated requests to the HF Hub」警告が出なくなる）。未インストール時は起動時の読み込みをスキップし、設定 > LLM の「埋め込みモデル」から一度だけダウンロード（インストール）できるようにした。
- 2026-07-07 17:10 リソースモニターのメーター間隔を均一に修正（値の固定幅予約をやめ、各メーターを自然幅で等間隔に詰めた）。RAM と GPU が近く見える問題を解消。
- 2026-07-07 17:00 設定の LLM に「コンテキスト長」スライダーを追加（4k / 8k / 16k / 32k / 64k / 128k / 256k から選択。次回のモデル起動時に反映）。ライブラリ切替の下矢印をフッター幅いっぱいに配置。資料の Web バッジの色を青からグレーに。デザインガイドラインを現状に更新。
- 2026-07-07 16:50 資料（RAG）の「＋」を追加メニューに変更（新規追加 / ファイル読み込み / ウェブ検索）。上部バーにあった Web 検索ボタンはこのメニューへ集約。「新規追加」で Markdown を書くエディタを開き、「保存してチャンク化」で段落ごとに分割して RAG に登録できる。
- 2026-07-07 16:25 上部バーを再配置: モデル選択バーを中央に丸い帯（LM Studio 風）で配置、設定 ⚙️ を右端に。リソースモニターの開閉ボタンを下部ステータスバーの右端に移動（バー自体は常時表示になり、そこから開閉）。資料（RAG）一覧の各行がボタンの既定枠で箱表示になっていたのを普通のテキスト行に修正。
- 2026-07-07 16:05 ライブラリ（データルート）切替を上部バーから左サイドバー下部のフッターに移動（Obsidian の Vault スイッチャー風）。設定（⚙️）ボタンも同フッターに配置。メニューは上向きに開く。
- 2026-07-07 15:55 サイドバーの改善: ①行の「…」メニュー / 「✕」ボタンを行のハイライト枠の中に収め、選択行では常に表示。②ドキュメント / 資料 / 画像の各セクションをフォルダのように見出しクリックで開閉できるように（開閉状態は記憶）。
- 2026-07-07 15:40 検索専用 LLM（ornith 9B / :8081）を廃止し、LLM を 1 つに一本化。Web 検索のクエリ分解・要約も文章用 LLM（:8080）で行うようにした。モデルバーの「検索LLM」と設定の「Web 検索用 LLM」項目を削除（設定の LLM は 1 つだけ選ぶ形に）。VRAM を二重に使わなくなる。
- 2026-07-07 15:20 分割ビュー（資料ペイン・執筆支援ペイン）と本文の境界をドラッグで動かせるようにした（280〜900px、幅は記憶）。スクロールバーを Obsidian 風の細い角丸・トラック透明のデザインに変更。
- 2026-07-07 15:05 左サイドバーの幅を右端のドラッグで変更できるようにした（180〜520px、幅は記憶される）。
- 2026-07-07 14:55 配色を Obsidian のデフォルトダークに寄せた（紙 #1e1e1e / 文字 #dadada / アクセント紫）。ダークテーマを基準デザインとし、初期設定もダークに。選択中のハイライトはニュートラルなグレーに変更。ライトテーマも同系の紫アクセントに更新。
- 2026-07-07 14:40 書式ツール（太字・見出し・リスト等）を、ドラッグで自由に動かせる横長のフローティングバーに変更。バーは本文エリアに付随し、執筆支援などのペイン開閉で本文が動くと一緒に動く。位置は記憶される。上部の固定バーは操作ツール（選択範囲を校正 / 全体を校正 / 執筆支援 / 履歴 / 保存）に。
- 2026-07-07 13:55 UI デザインを全面的に統一（モダンなテキストエディタ路線）。①角丸・文字サイズ・影・色をデザイントークン化し、ボタン/パネル/入力欄を共通スタイルに集約。アクセントは青 1 色（保存ボタン等も青に、緑は成功・稼働、赤は削除・エラー）。②ツールバーを 1 段に統合（左=書式、右=操作+保存）。③本文エリアをカード枠なしのフルブリードな紙に変更し、サイドバーはグレーのクロームに。フォーカスリングとホバーの挙動も統一。
- 2026-07-07 13:25 エディタの並びを「ツールバー → タイトル → 本文」に変更（従来はタイトルが最上部）。ツールバーは引き続きスクロール時も上部に固定。
- 2026-07-07 13:15 本文中の画像をクリックで選択すると、色付きの枠（アクセントカラー）で囲って選択中だと分かるようにした。
- 2026-07-07 13:10 「選択範囲を校正」の結果パネルが文書先頭に表示され、下の方をスクロール中だと見えなかった問題を修正。校正結果は固定ツールバーの直下に表示され、スクロール位置に関係なく確認できる。
- 2026-07-07 12:06 設定に「LLM」カテゴリを追加。文章用 / Web 検索用のモデルを models フォルダから選べる。検索用を「文章用と同じ」にすると 1 つの LLM で兼用（VRAM 節約、個別起動不要）。モデルバーは設定の既定モデルを初期選択し、兼用時は「文章用と共用」と表示。
- 2026-07-07 11:55 サイドバーの資料クリックで内容（要約ノート + 原文チャンク）を閲覧できるビューアを追加。画像サムネイルはクリックで拡大表示（挿入・削除ボタン付き）に変更。サイドバーをワークスペースツリー構造に再構成。

- 2026-07-07 10:52 アプリのウィンドウを閉じると、backend・起動中の LLM（gemma / ornith）・コンソールウィンドウもすべて自動終了するように。アイコンをフラットな線画に統一。

- 2026-07-07 10:35 未保存マークを実差分ベースに変更（編集を元に戻すとマークが消え、ドラフト退避も取り消される）。右クリックでコピー / 貼り付けメニューを追加。リソースモニターの縦幅を縮小。

- 2026-07-07 10:30 左サイドバーに「資料（RAG）」と「画像」セクションを追加。取り込んだ資料の一覧（種類バッジ・チャンク数）表示と削除、テキスト / Markdown ファイルの追加ができる。画像はサムネイル一覧からクリックでカーソル位置に挿入、✕ で削除。

- 2026-07-07 10:24 ダークモードを追加。上部バー右端の ⚙️ から設定ウィンドウを開き、「外観」でライト/ダークを切替（今後テーマを追加予定）。「エディタ」で本文フォントサイズ、「Web 検索」で Tavily API キーも設定可能。設定は `~/.lm-text-editor/settings.json` に保存。

- 2026-07-07 10:03 ウィンドウのコンテンツ領域を 1920×1080 で起動するように変更。F12 でスクリーンショットを撮影し、ライブラリ直下の `screenshot/` に保存（保存時に通知）。上部バーのドロップダウン等に出るフォーカス枠を非表示に。

- 2026-07-07 09:56 下部にリソースモニターを追加（CPU / RAM / GPU / VRAM、1 秒更新）。上部バー右端の 📊 で表示/非表示を切替（設定は記憶される）。
- 2026-07-07 09:42 Web 検索に URL 直接指定を追加。検索結果のタイトル/URL クリックで既定のブラウザが開くように。エディタ外へのファイルドロップでアプリが壊れる問題を防止。

- 2026-07-07 09:30 Web 検索を追加。上部バーの「🔍 Web 検索」から検索し、結果を「取り込む」と本文が RAG に保存され、要約ノートも自動生成される（検索用 LLM「ornith」はモデルバーから起動）。検索は DuckDuckGo（キー不要）、TAVILY_API_KEY 設定時は Tavily を使用。

- 2026-07-07 09:08 書式ツールバーを追加。太字・斜体・下線・打ち消し・コード・見出し・リスト・引用・コードブロック・水平線・リンク・undo/redo をボタン操作でき、カーソル位置の書式がハイライトされる。

- 2026-07-07 08:56 保存を明示方式に変更（保存ボタン / Ctrl+S）。保存のたびに履歴が残り、「履歴」から差分表示と過去版の読み込みができる。未保存の編集は自動でドラフト退避され、次回オープン時に復元できる。
- 2026-07-07 08:56 サイドバーの各行に「…」メニューを追加。ワークスペース / ドキュメントの名前変更と削除ができる。

- 2026-07-07 08:35 ライブラリ機能を追加。データの保存先フォルダ（DB・画像・RAG）を「ライブラリ」として新規作成・切り替えできる。上部バー左のライブラリメニューから操作。

- 2026-07-07 08:24 RAG を追加。資料の取り込み（API / 一括投入 CLI）と hybrid 検索（意味検索 + 全文検索）ができ、執筆支援の「RAG」チェックで検索結果を参照した生成が可能に。

- 2026-07-07 08:14 画面上部にモデルバーを追加。アプリ内から LLM モデルの選択・起動・切替・停止ができ、状態（未起動/起動中/稼働中）が常時表示される。start-llm.bat を手動実行しなくてもよくなった。

- 2026-07-07 08:05 分割ビュー校正を追加。2 段落以上の選択（または「文書全体を校正」）で左右比較ビューが開き、段落ごとに採用/スキップ、一括採用ができる。

- 2026-07-07 07:58 執筆支援を追加。「続きを生成」と「指示からセクション生成」ができ、結果をカーソル位置に挿入できる。LLM の思考モードを無効化し応答を高速化（校正 0.7 秒前後）。

- 2026-07-07 07:47 インライン校正を追加。選択範囲を Gemma 4 で校正し、diff 表示から採用/破棄できる。LLM サーバは `start-llm.bat` で起動。

- 2026-07-07 07:37 `start.bat` を追加。ダブルクリックで backend（別ウィンドウ）と Vite + Electron を一括起動。

- 2026-07-07 07:32 エディタ基盤を実装。ワークスペース / ドキュメント管理（サイドバー）、SQLite への自動保存、画像のペースト / ドロップ挿入、FastAPI backend（`npm run backend` で起動）。
- 2026-07-07 07:23 アプリ雛形を作成（Electron + Vite + React + TypeScript、TipTap v3 エディタの基本表示）。`npm run dev` / `npm run build` / `npm start` が使用可能に。
